<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <!-- Edge-to-edge: don't let Android auto-paint a contrast scrim behind the
         nav bar. That scrim reads as a light bar over dark screens/modals (the
         modal's edge-to-edge dark surface flashes correct, then the scrim
         repaints over it). Icon tint is controlled manually via
         NavigationBar.setButtonStyleAsync in app/_layout.tsx, so the scrim isn't
         needed for legibility. Mirrors expo.androidNavigationBar.enforceContrast
         = false in app.json (this project builds the committed android/ folder
         directly — no prebuild — so this XML is the source of truth). -->
    <item name="android:enforceNavigationBarContrast" tools:targetApi="29">false</item>
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#ffffff</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
    <item name="windowSplashScreenBackground">@color/splashscreen_background</item>
    <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen_logo</item>
    <item name="postSplashScreenTheme">@style/AppTheme</item>
    <item name="android:windowSplashScreenBehavior">icon_preferred</item>
  </style>
</resources>