Check the quorum-desktop or quorum-shared repo according to the user request:

$ARGUMENTS

- Local path to quorum-desktop: `..\quorum-desktop` (React web app)
- Local path to quorum-shared: `..\quorum-shared` (shared components/utilities used by desktop & mobile)

Context: `.agents\docs\quorum-shared-architecture.md`